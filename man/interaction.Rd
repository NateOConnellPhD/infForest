% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interaction.R
\name{interaction}
\alias{interaction}
\alias{interaction.infForest}
\title{Estimate Interactions Between Predictors}
\usage{
interaction(object, ...)

\method{interaction}{infForest}(
  object,
  var,
  by,
  at = c(0.25, 0.75),
  type = c("quantile", "value"),
  by_at = list(c(0.1, 0.25), c(0.75, 0.9)),
  bw = 20L,
  q_lo = 0.1,
  q_hi = 0.9,
  subset = NULL,
  ...
)
}
\arguments{
\item{object}{An \code{infForest} object fitted with \code{honesty = TRUE}.}

\item{...}{Additional arguments (currently unused).}

\item{var}{Character; name of the focal predictor variable.}

\item{by}{Character; name of the conditioning variable.}

\item{at}{Comparison points for the focal variable (continuous only).
Default \code{c(0.25, 0.75)}. See \code{\link{effect}} for details.}

\item{type}{How to interpret \code{at}: \code{"quantile"} (default) or
\code{"value"}.}

\item{by_at}{Subgroup definitions for a continuous \code{by} variable.
A list of 2-element numeric vectors, each defining a quantile range
(e.g., \code{list(c(0.10, 0.25), c(0.75, 0.90))}). Ignored for binary
\code{by}. Default: \code{list(c(0.10, 0.25), c(0.75, 0.90))}.}

\item{bw}{Bandwidth for continuous focal variable estimation. Default 20.}

\item{q_lo, q_hi}{Grid bounds for the focal variable. Default 0.10 and 0.90.}

\item{subset}{Optional integer vector of observation indices to restrict
estimation to. Intersected with the by-variable subgroups. Useful for
higher-order interactions: \code{int(fit, "x2", by = "x6", subset = which(dat$x7 == 1))}.}
}
\value{
A list of class \code{infForest_interaction} containing:
\describe{
  \item{variable}{Focal variable name.}
  \item{by}{Conditioning variable name.}
  \item{subgroups}{Data frame of subgroup-specific effects.}
  \item{differences}{Data frame of all pairwise differences.}
}
}
\description{
Computes the effect of a focal predictor within subgroups defined by a
conditioning (\code{by}) variable. Returns subgroup-specific effects and
all pairwise differences. Supports binary x binary, continuous x binary,
and continuous x continuous interactions.
}
\examples{
\dontrun{
fit <- infForest(y ~ ., data = dat)
int(fit, "x1", by = "x2")
int(fit, "x1", by = "x3", by_at = list(c(0.10, 0.33), c(0.33, 0.67), c(0.67, 0.90)))
int(fit, "x2", by = "x6", subset = which(dat$x7 == 1))
}

}
