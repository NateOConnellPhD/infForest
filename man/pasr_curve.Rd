% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pasr_curve.R
\name{pasr_curve}
\alias{pasr_curve}
\title{PASR Confidence Bands for Effect Curves}
\usage{
pasr_curve(
  object,
  var,
  q_lo = 0.1,
  q_hi = 0.9,
  bw = 20L,
  ref = NULL,
  variance_method = c("both", "pasr", "sandwich"),
  R_min = 20L,
  R_max = 200L,
  batch_size = 10L,
  tol = 0.05,
  n_stable = 2L,
  B_mc = 500L,
  alpha = 0.05,
  nuisance = NULL,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{object}{An \code{infForest} object.}

\item{var}{Character; name of a continuous predictor.}

\item{q_lo, q_hi}{Quantiles defining the curve range. Default 0.10 and 0.90.}

\item{bw}{Bandwidth for grid density. Default 20.}

\item{ref}{Reference value (curve = 0 here). Default: median.}

\item{variance_method}{Character: \code{"both"} (default), \code{"pasr"},
or \code{"sandwich"}.}

\item{R_min, R_max}{Minimum/maximum PASR replicates. Default 20 and 200.}

\item{batch_size}{Replicates per convergence check. Default 10.}

\item{tol}{Convergence tolerance. Default 0.05.}

\item{n_stable}{Consecutive stable batches. Default 2.}

\item{B_mc}{Trees per paired forest. Default 500.}

\item{alpha}{Significance level. Default 0.05.}

\item{nuisance}{An \code{infForest_nuisance} object. If NULL, estimated.}

\item{verbose}{Print progress. Default FALSE.}

\item{...}{Additional arguments passed to \code{estimate_nuisance}.}
}
\value{
A list of class \code{infForest_curve} with added fields:
\describe{
  \item{se}{Pointwise SE at each grid point.}
  \item{ci_lower, ci_upper}{Pointwise confidence bands.}
  \item{cov_matrix}{Joint covariance matrix (PASR, when computed).}
  \item{omnibus_stat}{Chi-squared omnibus test statistic.}
  \item{omnibus_df}{Degrees of freedom for omnibus test.}
  \item{omnibus_pval}{Omnibus p-value.}
}
}
\description{
Estimates pointwise and simultaneous confidence bands for a nonlinear
effect curve using procedure-aligned synthetic resampling. Fits paired
forests once per replicate and extracts the full curve from each,
yielding the joint covariance matrix across all grid points. Also
computes the sandwich variance at each grid point and an omnibus
chi-squared test for the null of no association.
}
\details{
This is dramatically faster than calling \code{pasr_effect()} in a loop
over grid points: \code{R * 4} ranger fits total instead of
\code{G * R * 4}.
}
\examples{
\dontrun{
fit <- infForest(y ~ ., data = dat)
pc <- pasr_curve(fit, "x1", verbose = TRUE)
plot(pc)  # curve with confidence bands
pc$omnibus_pval  # global test of association
}

}
